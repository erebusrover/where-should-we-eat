import React from 'react';


const GroupMember = (props) => {
  const { groupMember } = props;
  return (
      <div>
          <h2>{}</h2>
          <h2>MemberStatus</h2>
      </div>

  );
};

export default GroupMember;