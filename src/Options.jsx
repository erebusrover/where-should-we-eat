import React from 'react';


const Options = (props) => {
  const { options } = props;
  return (
      <div>
          <h2>{}</h2>
          <h2>This has rendered </h2>
      </div>

  );
};

export default Options;